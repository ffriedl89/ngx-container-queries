import { ContainerQueryDirective } from './container-query.directive';

describe('ContainerQueryDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerQueryDirective();
    expect(directive).toBeTruthy();
  });
});
